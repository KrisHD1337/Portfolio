import Head from 'next/head'

function Title(title) {
    return (
        <div>
            <Head>
                <title>title</title>
            </Head>
        </div>
    )
}

export default Title